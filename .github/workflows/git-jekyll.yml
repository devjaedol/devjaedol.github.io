# This is a basic workflow to help you get started with Actions

name: GitAction-Jekyll-CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run PWD
        run: pwd

      - name: Run ls -al
        run: ls -al

